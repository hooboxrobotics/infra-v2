clusterName: hbx-prd
region: sa-east-1

serviceAccount:
  create: true
  name: aws-load-balancer-controller

replicaCount: 1

nodeSelector:
  workload: infra

resources:
  requests:
    cpu: "50m"
    memory: "64Mi"
  limits:
    cpu: "200m"
    memory: "256Mi"
