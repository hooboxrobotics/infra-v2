apiVersion: v1
kind: Service
metadata:
  name: app-frontend-suporte
  namespace: apps-app-frontend
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: app-frontend-suporte
