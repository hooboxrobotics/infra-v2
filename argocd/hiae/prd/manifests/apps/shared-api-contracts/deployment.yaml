apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: shared-api-contracts
  name: shared-api-contracts
  namespace: apps-shared-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shared-api-contracts
  template:
    metadata:
      labels:
        app: shared-api-contracts
    spec:
      containers:
      - image: 264221247403.dkr.ecr.sa-east-1.amazonaws.com/shared-api-contracts:8cf5e7
        imagePullPolicy: IfNotPresent
        name: shared-api-contracts-app
        ports:
        - containerPort: 3000
          protocol: TCP
        envFrom:
          - secretRef:
              name: shared-api-contracts
              optional: true
