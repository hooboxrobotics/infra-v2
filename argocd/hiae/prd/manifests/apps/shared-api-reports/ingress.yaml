apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shared-api-reports
  namespace: apps-shared-api
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    traefik.ingress.kubernetes.io/router.middlewares: traefik-redirect-to-https@kubernetescrd
spec:
  rules:
  - host: reports-api-hoobox.einstein.br
    http:
      paths:
      - backend:
          service:
            name: shared-api-reports
            port:
              number: 80
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - reports-api-hoobox.einstein.br
    secretName: reports-api-hoobox-one-tls
