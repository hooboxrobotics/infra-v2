apiVersion: v1
kind: Service
metadata:
  name: shared-api-reports
  namespace: apps-shared-api
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: shared-api-reports
