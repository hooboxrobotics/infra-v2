clusterName: hbx-prd
region: sa-east-1
# Explicito para nao depender do IMDS (hop limit dos nos bloqueia auto-discovery).
vpcId: vpc-00e9049034b834392

serviceAccount:
  create: true
  name: aws-load-balancer-controller

replicaCount: 1

nodeSelector:
  workload: infra

resources:
  requests:
    cpu: "50m"
    memory: "64Mi"
  limits:
    cpu: "200m"
    memory: "256Mi"
