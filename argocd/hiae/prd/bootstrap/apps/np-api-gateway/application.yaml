apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: np-api-gateway
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: apps
  source:
    repoURL: https://github.com/hooboxrobotics/infra-v2.git
    targetRevision: main
    path: argocd/hiae/prd/manifests/apps/np-api-gateway
  destination:
    server: https://kubernetes.default.svc
    namespace: np-api-gateway
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
